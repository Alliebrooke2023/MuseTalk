[pytest]
# Repo root on sys.path so `import musetalk.utils.*` resolves (musetalk is an
# implicit namespace package — there is no top-level musetalk/__init__.py).
pythonpath = .
testpaths = tests
addopts = -ra
